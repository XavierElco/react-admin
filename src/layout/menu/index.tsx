import styles from './index.module.less';

export default function menu() {
    return (
        <div className={styles.menuContainer}>
            <div className={styles.menuTitle}>菜单栏开发中。。。</div>
        </div>
    )
}